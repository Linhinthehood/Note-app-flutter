name: note_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2       # Moved from dev_dependencies
  sqflite: ^2.3.2        # Moved from dev_dependencies  
  path: ^1.9.0           # Moved from dev_dependencies
  intl: ^0.19.0          # Moved from dev_dependencies

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.4        # Add for testing
  build_runner: ^2.4.9 
  sqflite_common_ffi: ^2.3.6  # Add for code generation

flutter:
  uses-material-design: true